import React from 'react'

export const NavHome = ({textNav, ref}) => {
  return (

    <nav>
        <a href='#'>{textNav}</a>
        <a href='#'>{textNav}</a>
        <a href='#'>{textNav}</a>


    </nav>
  )
}
